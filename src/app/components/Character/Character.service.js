/*

This will be the service that allows people to import character sheet json to set up the add form.
This may be a part of a smaller service / controller that sets up the whole application with json settings.

(function() {
    'use strict';

    angular
        .module('initiativeJs')
        .service('Character', Character);
*/
    /** @ngInject */
/*    function Character() {

        var char = {
            name: '',
            toughness: '',
            parry: '',
            quick: false
        }

        this.getCharacter = getCharacter;

        function getCharacter(){
            return char;
        }
    }
})();*/
