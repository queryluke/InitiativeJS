/*

This will be the file that allows dynamic form generation from JSON.

However there are some very good plugins that do this. Will look into those:
 https://github.com/danhunsaker/angular-dynamic-forms
 http://schemaform.io/

(function() {
    'use strict';

    angular
        .module('initiativeJs')
        .factory('formGenerator', formGenerator);
*/
    /** @ngInject */
/*    function formGenerator() {

        var directive = {
            restrict: 'E',
            templateUrl: function(elem, attr){
                return 'app/components/formGenerator/'+attr.type+'.html';
            }
        };

        return directive;
    }
})();
*/